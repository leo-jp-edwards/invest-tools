codecov:
  branch: main
  notify:
    after_n_builds: 10
comment: true

coverage:
  status:
    project:
      default:
        target: '82'
    patch:
      default:
        target: '50'
        informational: true

github_checks:
    annotations: true

ignore:
  - "tests"